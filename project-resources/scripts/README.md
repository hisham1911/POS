# 🛠️ Scripts - السكريبتات

مجموعة من السكريبتات المساعدة لإدارة وصيانة مشروع KasserPro.

---

## 📂 التقسيمات (Directory Structure)

### 🗄️ [`database/`](./database/)
**سكريبتات قاعدة البيانات**
- ملفات SQL للإصلاحات والصيانة
- سكريبتات إعادة تعيين قاعدة البيانات
- سكريبتات إصلاح المخزون

**الملفات:**
- `*.sql` - استعلامات SQL مختلفة
- `reset-database.ps1` - إعادة تعيين قاعدة البيانات
- `fix-inventory.ps1` - إصلاح بيانات المخزون

**الاستخدام:**
```powershell
cd scripts/database
.\reset-database.ps1
```

---

### 🧪 [`testing/`](./testing/)
**سكريبتات الاختبار**
- اختبارات API
- اختبارات التكامل
- سكريبتات التحقق

**الملفات:**
- `test-*.ps1` - سكريبتات اختبار PowerShell
- `test-*.http` - ملفات اختبار REST API
- `verify-*.ps1` - سكريبتات التحقق

**الاستخدام:**
```powershell
cd scripts/testing
.\test-system.ps1
```

أو استخدم ملفات `.http` مع VS Code REST Client extension

---

### 🚀 [`deployment/`](./deployment/)
**سكريبتات النشر**
- سكريبت البناء والنشر الآلي
- سكريبتات التحضير للإنتاج

**الملف الرئيسي:**
- `build-and-deploy.ps1` - بناء ونشر كامل المشروع

**الاستخدام:**
```powershell
cd scripts/deployment
.\build-and-deploy.ps1 -Version "1.0.0"
```

---

### 🔧 [`maintenance/`](./maintenance/)
**سكريبتات الصيانة**
- إعادة تعيين البيانات
- التنظيف والصيانة الدورية

**الملفات:**
- `reset-data.ps1` - إعادة تعيين البيانات التجريبية

**الاستخدام:**
```powershell
cd scripts/maintenance
.\reset-data.ps1
```

---

## ⚠️ تحذيرات هامة

### قبل تشغيل أي سكريبت:

1. **اقرأ الكود أولاً!** 📖
   - افتح الملف واقرأ ما يفعله
   - تأكد أنك تفهم النتيجة

2. **عمل Backup!** 💾
   - سكريبتات قاعدة البيانات قد تحذف البيانات
   - احفظ نسخة احتياطية من `kasserpro.db` قبل التشغيل

3. **تحقق من البيئة** 🌍
   - تأكد أنك تعمل على environment المناسب (Development/Production)
   - بعض السكريبتات لا يجب تشغيلها على Production!

4. **أوقف الخدمات** ⏸️
   - أوقف Backend قبل تشغيل سكريبتات قاعدة البيانات
   - أوقف Frontend إذا لزم الأمر

---

## 🚀 سكريبتات مهمة

### إعادة تعيين المشروع بالكامل
```powershell
# 1. إعادة تعيين قاعدة البيانات
cd scripts/database
.\reset-database.ps1

# 2. إعادة تعيين البيانات التجريبية
cd ../maintenance
.\reset-data.ps1

# 3. اختبار النظام
cd ../testing
.\test-system.ps1
```

### بناء ونشر للإنتاج
```powershell
cd scripts/deployment
.\build-and-deploy.ps1 -Version "1.0.0"
```

---

## 📝 ملاحظات

- **PowerShell Scripts:** تتطلب PowerShell 5.1 أو أحدث
- **HTTP Scripts:** تتطلب VS Code مع REST Client extension
- **SQL Scripts:** يمكن تشغيلها عبر SQLite CLI أو DB Browser

---

## 🤝 المساهمة

عند إضافة سكريبت جديد:
1. ضعه في المجلد المناسب
2. أضف تعليقات واضحة في الكود
3. حدّث هذا الـ README
4. اختبر السكريبت قبل الـ commit

---

**آخر تحديث:** 16 فبراير 2026
