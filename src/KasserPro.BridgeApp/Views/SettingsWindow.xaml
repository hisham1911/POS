<Window x:Class="KasserPro.BridgeApp.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KasserPro Bridge - إعدادات" 
        Height="700" 
        Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinHeight="650"
        MinWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Backend URL -->
            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <Label Content="عنوان الخادم - Backend URL:" FontWeight="Bold" FontSize="14"/>
                <TextBox x:Name="BackendUrlTextBox" Height="35" Padding="8" FontSize="13"/>
                <TextBlock Text="مثال: https://localhost:5243" 
                           Foreground="Gray" 
                           FontSize="11" 
                           Margin="0,3,0,0"/>
            </StackPanel>

            <!-- API Key -->
            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <Label Content="مفتاح API - API Key:" FontWeight="Bold" FontSize="14"/>
                <TextBox x:Name="ApiKeyTextBox" Height="35" Padding="8" FontSize="13"/>
                <TextBlock Text="للاختبار: test-api-key-123" 
                           Foreground="Gray" 
                           FontSize="11" 
                           Margin="0,3,0,0"/>
            </StackPanel>

            <!-- Device ID (Read-only) -->
            <StackPanel Grid.Row="2" Margin="0,0,0,15">
                <Label Content="معرف الجهاز - Device ID:" FontWeight="Bold" FontSize="14"/>
                <TextBox x:Name="DeviceIdTextBox" Height="35" Padding="8" FontSize="13" IsReadOnly="True" Background="#F5F5F5"/>
            </StackPanel>

            <!-- Default Printer -->
            <StackPanel Grid.Row="3" Margin="0,0,0,15">
                <Label Content="الطابعة الافتراضية - Default Printer:" FontWeight="Bold" FontSize="14"/>
                <ComboBox x:Name="PrinterComboBox" Height="35" FontSize="13" Padding="5"/>
                <Button x:Name="RefreshPrintersButton" 
                        Content="تحديث قائمة الطابعات - Refresh Printers" 
                        Width="250" 
                        Height="30" 
                        Margin="0,8,0,0" 
                        HorizontalAlignment="Left"
                        FontSize="12"
                        Click="RefreshPrintersButton_Click"/>
            </StackPanel>

            <!-- Receipt Settings -->
            <GroupBox Grid.Row="4" Header="إعدادات الفاتورة - Receipt Settings" 
                      FontWeight="Bold" FontSize="14" Margin="0,0,0,15" Padding="10">
                <StackPanel>
                    <!-- Font Size -->
                    <StackPanel Margin="0,5,0,10">
                        <Label Content="حجم الخط العادي - Regular Font Size:" FontWeight="Normal" FontSize="12"/>
                        <StackPanel Orientation="Horizontal">
                            <Slider x:Name="RegularFontSizeSlider" Minimum="6" Maximum="12" Value="8" 
                                    Width="200" VerticalAlignment="Center" TickFrequency="1" IsSnapToTickEnabled="True"/>
                            <TextBlock x:Name="RegularFontSizeText" Text="8" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Margin="0,5,0,10">
                        <Label Content="حجم الخط العريض - Bold Font Size:" FontWeight="Normal" FontSize="12"/>
                        <StackPanel Orientation="Horizontal">
                            <Slider x:Name="BoldFontSizeSlider" Minimum="7" Maximum="14" Value="9" 
                                    Width="200" VerticalAlignment="Center" TickFrequency="1" IsSnapToTickEnabled="True"/>
                            <TextBlock x:Name="BoldFontSizeText" Text="9" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Margin="0,5,0,10">
                        <Label Content="حجم خط العنوان - Header Font Size:" FontWeight="Normal" FontSize="12"/>
                        <StackPanel Orientation="Horizontal">
                            <Slider x:Name="HeaderFontSizeSlider" Minimum="9" Maximum="16" Value="11" 
                                    Width="200" VerticalAlignment="Center" TickFrequency="1" IsSnapToTickEnabled="True"/>
                            <TextBlock x:Name="HeaderFontSizeText" Text="11" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Line Spacing -->
                    <StackPanel Margin="0,5,0,10">
                        <Label Content="المسافة بين الأسطر - Line Spacing:" FontWeight="Normal" FontSize="12"/>
                        <StackPanel Orientation="Horizontal">
                            <Slider x:Name="LineSpacingSlider" Minimum="0.8" Maximum="1.5" Value="1.0" 
                                    Width="200" VerticalAlignment="Center" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                            <TextBlock x:Name="LineSpacingText" Text="1.0" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Checkboxes -->
                    <CheckBox x:Name="ShowBranchNameCheckBox" Content="إظهار اسم الفرع - Show Branch Name" 
                              IsChecked="True" Margin="0,5,0,5" FontSize="12"/>
                    <CheckBox x:Name="ShowBarcodeCheckBox" Content="إظهار الباركود - Show Barcode" 
                              IsChecked="True" Margin="0,5,0,5" FontSize="12"/>
                    <CheckBox x:Name="ShowThankYouCheckBox" Content="إظهار رسالة الشكر - Show Thank You" 
                              IsChecked="True" Margin="0,5,0,5" FontSize="12"/>
                </StackPanel>
            </GroupBox>

            <!-- Connection Status -->
            <StackPanel Grid.Row="5" Margin="0,0,0,15">
                <Label Content="حالة الاتصال - Connection Status:" FontWeight="Bold" FontSize="14"/>
                <TextBlock x:Name="ConnectionStatusTextBlock" 
                           Text="غير متصل - Disconnected" 
                           Foreground="Red"
                           FontSize="15"
                           FontWeight="Bold"
                           Margin="5,0,0,0"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="7" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        Margin="0,20,0,20">
                <Button x:Name="SaveButton" 
                        Content="💾 حفظ - Save" 
                        Width="150" 
                        Height="45" 
                        Margin="0,0,20,0"
                        FontSize="14"
                        FontWeight="Bold"
                        Background="#4CAF50"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="SaveButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#4CAF50"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#45a049"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button x:Name="CancelButton" 
                        Content="❌ إلغاء - Cancel" 
                        Width="150" 
                        Height="45"
                        FontSize="14"
                        FontWeight="Bold"
                        Background="#f44336"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="CancelButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#f44336"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#da190b"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
